#include <iostream>
#include <vector>
#include <string>
std::string LCP(std::vector<std::string>vec){
	std::prefix = vec[0];//per stroka
	for(size_t i = 1;i < 100;++i){ //proxod po vsem strokam
		size_t j = ;i <
		while(j <  
int main(){
	return 0;
}
